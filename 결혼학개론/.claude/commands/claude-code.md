Use the claude-code-guide agent to answer the user's question about Claude Code.

The claude-code-guide agent specializes in:
- Claude Code CLI features, settings, hooks, slash commands
- MCP servers and IDE integrations
- Keyboard shortcuts and keybindings
- Claude Agent SDK for building custom agents
- Claude API / Anthropic SDK usage, tool use, prompt caching

Delegate the user's question to the claude-code-guide agent and return its answer.
